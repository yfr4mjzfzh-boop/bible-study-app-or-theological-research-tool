# Theos Logos — BUGS-PLAN (Chief Boss numbering — authoritative)

Updated 2026-09-05 ~23:25 ET

**Grok:** Ignore remapped overnight IDs. Use symptoms.

## DONE
- BUG-1 retap verse (`4c6adf`)
- BUG-6 dim `pointer-events: auto` when open; Escape via workspace
- BUG-7 mobile drawer `w-[calc(100%-2.75rem)]` peek strip
- BUG-8 `--motion-drawer: 500ms` slide-first (no opacity pop)
- BUG-2 TOC clear-if-same
- BUG-3 marked chips clear-if-same
- BUG-9 type-menu stay mounted + `data-open` exit (~320ms)
- BUG-10 xl reception `deskShown`/`deskOpen` exit (~520ms)

## HELD / ongoing
- BUG-4 desk seats (product)
- BUG-5 live bleed fixtures

## Motion guide
Micro ~150ms · Panels ~500ms · Enter = exit · Dim clickable when open.
